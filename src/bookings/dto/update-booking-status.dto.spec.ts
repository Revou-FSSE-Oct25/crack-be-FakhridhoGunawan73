import { UpdateBookingStatusDto } from './update-booking-status.dto';

describe('UpdateBookingStatusDto', () => {
  it('should be defined', () => {
    expect(new UpdateBookingStatusDto()).toBeDefined();
  });
});
